{
  "name": "whire-be",
  "version": "1.0.0",
  "description": "demo Assignment Backend",
  "main": "./dist/index.js",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "utils/*"
    ]
  },
  "scripts": {
    "clean:light": "rm -rf ./tsconfig.build.tsbuildinfo && rm -rf ./dist",
    "clean:full": "yarn run clean:light && rm -rf ./node_modules",
    "test:watch": "jest --watch --runInBand --colors",
    "test": "jest --coverage --runInBand --colors --bail --detectOpenHandles",
    "test:with:env": "yarn local:db:start:test:db && yarn test && yarn local:db:force:stop",
    "local:db:start:test:db": "lerna run start:test:db --scope @whire-be/local-test-server --stream",
    "local:db:force:stop": "lerna run force:stop --scope @whire-be/local-test-server --stream",
    "lint": "yarn eslint './{utils,packages}/**/src/**/*.ts'",
    "pretty": "yarn prettier --write './{utils,packages}/**/src/**/*.ts'",
    "typecheck": "yarn lerna run typecheck --parallel",
    "start": "lerna run start --stream --parallel",
    "start:local:server": "lerna run start --stream --parallel --scope @whire-be/local-lambda-server --scope @whire-be/lambda-gq-resolver",
    "force:stop": "lerna run force:stop --stream --parallel",
    "nodemon": "nodemon",
    "build": "lerna run build",
    "build:prod": "NODE_ENV=production yarn build",
    "cdk:synth": "lerna run synth --scope @whire-be/cdk-app && rm -rf cdk.out && cp -r packages/cdk-app/cdk.out cdk.out"
  },
  "author": "Simon Verhoeven",
  "license": "MIT",
  "dependencies": {
    "@types/jest": "^27.0.2",
    "@types/uuid": "^8.3.1",
    "express": "^4.17.1",
    "npm-run-all": "^4.1.5",
    "tsconfig-paths": "^3.11.0",
    "tsconfig-paths-jest": "^0.0.1",
    "typescript": "^4.4.3",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.7",
    "@typescript-eslint/eslint-plugin": "^4.31.2",
    "@typescript-eslint/parser": "^4.31.2",
    "eslint": "^7.32.0",
    "eslint-config-airbnb": "^18.2.1",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.24.2",
    "eslint-plugin-jsx-a11y": "^6.4.1",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-plugin-react": "7.21.5",
    "eslint-plugin-react-hooks": "^4.3.0",
    "husky": "^4.3.8",
    "jest": "^27.0.0",
    "lint-staged": "^11.1.2",
    "prettier": "^2.4.1",
    "ts-jest": "^27.0.5",
    "ts-node": "^10.4.0",
    "tsconfig-paths": "^3.11.0",
    "ttypescript": "^1.5.12",
    "typescript": "^4.4.3",
    "typescript-transform-paths": "^2.0.0"
  },
  "resolutions": {
    "aws-sdk": "2.965.0"
  },
  "lint-staged": {
    "{packages,utils}/*/src/**/*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix",
      "jest --bail --runInBand --findRelatedTests"
    ],
    "{packages,utils}/*/src/**/*.{json,ts,tsx,html,css,scss}": [
      "prettier --write --config ./.prettierrc"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && lerna run precommit --parallel --since HEAD"
    }
  },
  "packageManager": "yarn@3.1.0"
}
